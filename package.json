{
  "name": "twap-ui-root",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "5.x"
  },
  "scripts": {
    "clean": "lerna clean -y || true",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "start": "lerna run start --parallel"
  },
  "prettier": {
    "printWidth": 180
  }
}
