{
  "name": "@orbs-network/twap-ui-dapp-example",
  "version": "0.0.1",
  "private": true,
  "homepage": "https://orbs-network.github.io/twap-ui/",
  "scripts": {
    "prettier": "prettier -w 'src/**/*.{ts,tsx,js,jsx,json,sol}'",
    "start": "npm run prettier && react-scripts start",
    "build": "npm run prettier && react-scripts build"
  },
  "dependencies": {
    "@orbs-network/twap-ui-spiritswap": "*",
    "react-query": "*"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
