import React from "react";
import Avatar from "@mui/material/Avatar";

function TokenLogo({ address }: { address: string }) {
  return <Avatar className="twap-token-logo" alt="token logo" src={""} />;
}

export default TokenLogo;
